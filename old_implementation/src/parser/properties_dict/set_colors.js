function	set_green(obj, nb)
{
    obj.green = nb;
    return (true);
}

function	set_red(obj, nb)
{
    obj.red = nb;
    return (true);
}

function	set_blue(obj, nb)
{
    obj.blue = nb;
    return (true);
}
