function	set_x_rot(obj, nb)
{
    obj.x_rot = nb;
    return (true);
}

function	set_y_rot(obj, nb)
{
    obj.y_rot = nb;
    return (true);
}

function	set_z_rot(obj, nb)
{
    obj.z_rot = nb;
    return (true);
}
