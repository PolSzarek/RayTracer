function	set_x(obj, nb)
{
    obj.x = nb;
    return (true);
}

function	set_y(obj, nb)
{
    obj.y = nb;
    return (true);
}

function	set_z(obj, nb)
{
    obj.z = nb;
    return (true);
}

function	set_height(obj, nb)
{
    obj.height = nb;
    return (true);
}

function	set_width(obj, nb)
{
    obj.width = nb;
    return (true);
}
